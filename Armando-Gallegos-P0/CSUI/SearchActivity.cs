using System;
using CSBL;
using CSModels;

namespace CSUI
{
    /*
    public class SearchActivity :ISMenu
    {
        private static Activity _act = new Activity();
        private IActivityBL _actBL;

        public SearchActivity(IActivityBL p_actBL)
        {
            /* Console.WriteLine("\nContains: Activity with Name=Checkers: {0}", 
                    act.contains(new act);
                    act.Contains(new Activity { Activity = "Checkers", PartName = "" }));       
                    // Find items where name contains "seat".        
                    Console.WriteLine("\nFind: Part where name contains \"seat\": {0}",           
                     parts.Find(x => x.PartName.Contains("seat")));       
                      // Check if an item with Id 1444 exists.       
                       Console.WriteLine("\nExists: Part with Id=1444: {0}",           
                        parts.Exists(x => x.PartId == 1444));
        }
        public void Menu()
        {
            bool correct = false;
            string answer;
            while(!correct)
            {
                Console.WriteLine("Searching for an Activity");
                Console.Write("Activity Name: ");
                _act.AName = Console.ReadLine();
                Console.WriteLine("Search Results:");
                Console.WriteLine("================");
                Console.WriteLine("Activity Name: " + _act.AName);
                Console.WriteLine("Activity Description: " + _act.ADescription);
                Console.WriteLine("=================");
                Console.WriteLine("Is this the Activity that you were searching for?");
                Console.WriteLine("Enter [Y] for Yes or [N] to search for the activity again.");
                answer = Console.ReadLine();
                if(answer == "Y")
                {
                    correct = true;
                    _actBL.AddActivity(_act);
                }
                else 
                    Console.Clear();
            }
        }
        
        public MenuChoices UserChoice()
        {
            return MenuChoices.AdminMenu;
        }
    }*/
}  
    
        
